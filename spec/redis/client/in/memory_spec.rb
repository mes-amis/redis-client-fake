# frozen_string_literal: true

RSpec.describe Redis::Client::In::Memory do
  it "has a version number" do
    expect(Redis::Client::In::Memory::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
